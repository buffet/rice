#
# wm independent hotkeys
#

# spawn terminal
super + Return
	$TERMINAL

# reload sxhkd
super + Escape
	pkill -USR1 -x sxhkd

# lock screen
super + shift + x
	slock

#
# kiwmi hotkeys
#

# quit kiwmi
super + shift + q
	seed quit

# reload kiwmi
super + shift + r
	seed reload

# exec kiwmi
super + alt + r
	seed exec kiwmi
